import styled from 'styled-components';

const InputOutputContent = styled.div`
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
`;

const ContentContainer = styled.div``;

export { InputOutputContent, ContentContainer };
